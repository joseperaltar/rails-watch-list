<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="navbar p-3 bg-dark">
      <%= link_to home_path do %>
        <i class="fa-solid fa-ticket fs-5 text-white"> MOBEES</i>
      <% end %>
      <div>
        <%= link_to "My lists", lists_path, class: "text-white text-decoration-none mx-2" %>
        <%= link_to "New list", new_list_path, class: "text-white text-decoration-none" %>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
